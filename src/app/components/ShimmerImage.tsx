// Kept as a re-export for backwards compatibility.
// SmartImage replaces the quality-crushing ShimmerImage while preserving
// the blur placeholder behavior (now with sane default quality).
export { default } from '@/app/components/SmartImage'
